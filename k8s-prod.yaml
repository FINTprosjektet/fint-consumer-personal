apiVersion: v1
kind: Service
metadata:
  annotations: {service.beta.kubernetes.io/azure-load-balancer-internal: 'true'}
  labels: {fint.stack: administrasjon-personal, fint.role: consumer}
  name: consumer-administrasjon-personal
spec:
  type: LoadBalancer
  ports:
    - {name: '8080', port: 8080}
  selector: {fint.stack: administrasjon-personal, fint.role: consumer}
---
apiVersion: v1
kind: Service
metadata:
  annotations: {service.beta.kubernetes.io/azure-load-balancer-internal: 'true'}
  labels: {fint.stack: administrasjon-personal, fint.role: provider}
  name: provider-administrasjon-personal
spec:
  type: LoadBalancer
  ports:
    - {name: '8080', port: 8080}
  selector: {fint.stack: administrasjon-personal, fint.role: provider}
---
apiVersion: apps/v1
kind: Deployment
metadata:
  labels: {fint.stack: administrasjon-personal, fint.role: consumer}
  name: consumer-administrasjon-personal
spec:
  replicas: 1
  selector:
    matchLabels: {fint.stack: administrasjon-personal, fint.role: consumer}
  strategy:
    type: RollingUpdate
    rollingUpdate: {maxSurge: 1, maxUnavailable: 0}
  template:
    metadata:
      labels: {fint.stack: administrasjon-personal, fint.role: consumer}
    spec:
      containers:
        - env:
            - {name: TZ, value: Europe/Oslo}
            - {name: JAVA_TOOL_OPTIONS, value: '-XX:+ExitOnOutOfMemoryError -XX:+UseG1GC -Xmx32G -verbose:gc'}
            - {name: fint.audit.mongo.databasename, value: fint-audit}
            - name: fint.audit.mongo.connection-string
              valueFrom:
                secretKeyRef: {key: mongodb, name: fint-audit}
            - {name: fint.audit.mongo.collection, value: auditEvents}
            - {name: fint.audit.mongo.buffer-size, value: '125000'}
            - {name: fint.audit.mongo.rate, value: '1000'}
            - {name: fint.cache.manager, value: default}
            - {name: fint.events.orgIds, value: health.fintlabs.no}
            - {name: fint.hazelcast.kubernetes.enabled, value: 'true'}
            - {name: fint.hazelcast.kubernetes.namespace, value: default}
            - {name: fint.hazelcast.kubernetes.labelName, value: fint.stack}
            - {name: fint.hazelcast.kubernetes.labelValue, value: administrasjon-personal}
            - {name: fint.relations.default-base-url, value: 'https://api.felleskomponent.no'}
            - {name: server.context-path, value: /administrasjon/personal}
            - {name: spring.mvc.logResolvedException,  value: 'false'}
            - {name: endpoints.sensitive, value: 'false'}
            - {name: management.security.enabled, value: 'false'}
          image: fintlabs.azurecr.io/consumer-administrasjon-personal:3.3.1
          name: consumer-administrasjon-personal
          ports:
            - {containerPort: 8080}
          readinessProbe:
            httpGet: {path: /administrasjon/personal/health, port: 8080}
            initialDelaySeconds: 60
            timeoutSeconds: 30
          resources:
            requests: {memory: 21Gi, cpu: 250m}
            limits: {memory: 36Gi, cpu: '6'}
      restartPolicy: Always
---
apiVersion: apps/v1
kind: Deployment
metadata:
  labels: {fint.stack: administrasjon-personal, fint.role: provider}
  name: provider-administrasjon-personal
spec:
  replicas: 1
  selector:
    matchLabels: {fint.stack: administrasjon-personal, fint.role: provider}
  strategy:
    type: RollingUpdate
    rollingUpdate: {maxSurge: 1, maxUnavailable: 0}
  template:
    metadata:
      labels: {fint.stack: administrasjon-personal, fint.role: provider}
    spec:
      containers:
        - env:
            - {name: TZ, value: Europe/Oslo}
            - {name: JAVA_TOOL_OPTIONS, value: '-XX:+ExitOnOutOfMemoryError -Xmx10G -verbose:gc'}
            - {name: fint.audit.mongo.databasename, value: fint-audit}
            - {name: fint.audit.mongo.collection, value: auditEvents}
            - name: fint.audit.mongo.connection-string
              valueFrom:
                secretKeyRef: {key: mongodb, name: fint-audit}
            - {name: fint.audit.mongo.buffer-size, value: '125000'}
            - {name: fint.audit.mongo.rate, value: '1000'}
            - {name: fint.events.orgIds, value: health.fintlabs.no}
            - {name: fint.hazelcast.kubernetes.enabled, value: 'true'}
            - {name: fint.hazelcast.kubernetes.namespace, value: default}
            - {name: fint.hazelcast.kubernetes.labelName, value: fint.stack}
            - {name: fint.hazelcast.kubernetes.labelValue, value: administrasjon-personal}
            - {name: fint.provider.assets.endpoint, value: 'https://admin-beta.fintlabs.no/api/components/assets/administrasjon_personal'}
            - {name: fint.provider.max-number-of-emitters, value: '5'}
            - {name: fint.provider.sse.heartbeat.enabled, value: 'false'}
            - {name: server.context-path, value: /administrasjon/personal/provider}
            - {name: server.tomcat.max-threads, value: '400'}
            - {name: server.tomcat.max-connections, value: '30000'}
            - {name: endpoints.sensitive, value: 'false'}
            - {name: management.security.enabled, value: 'false'}
          image: fintlabs.azurecr.io/provider:2.2.0
          name: provider-administrasjon-personal
          ports:
            - {containerPort: 8080}
          readinessProbe:
            httpGet: {path: /administrasjon/personal/provider/health, port: 8080}
            initialDelaySeconds: 60
            timeoutSeconds: 30
          resources:
            requests: {memory: 10Gi, cpu: 250m}
            limits: {memory: 12Gi, cpu: 2000m}
      restartPolicy: Always
