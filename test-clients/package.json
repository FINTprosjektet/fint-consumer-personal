{
  "name": "test-clients",
  "scripts": {
    "start": "node test-client.js",
    "test-clients": "concurrently --kill-others '../start-local-rabbitmq.sh' 'npm start'",
    "all": "concurrently --kill-others '../start-local-rabbitmq.sh' 'npm start' '../gradlew bootRun -p ../ -Dspring.profiles.active=test'"
  },
  "dependencies": {
    "amqplib": "^0.5.1",
    "concurrently": "^3.4.0",
    "sleep": "^5.1.0"
  }
}
