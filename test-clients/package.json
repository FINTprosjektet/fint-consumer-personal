{
  "name": "test-clients",
  "scripts": {
    "start": "node health.js",
    "all": "concurrently --kill-others '../start-local-rabbitmq.sh' 'node health.js' '../gradlew bootRun -p ../ -Dspring.profiles.active=test'"
  },
  "dependencies": {
    "amqplib": "^0.5.1",
    "concurrently": "^3.2.0",
    "sleep": "^5.1.0"
  }
}
